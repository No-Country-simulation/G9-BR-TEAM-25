# Oracle Autonomous Database (configure the real password only in the shell)
OCI_DATABASE_ENABLED=true
OCI_DATABASE_URL=jdbc:oracle:thin:@techminddb_high
OCI_DATABASE_USERNAME=ADMIN
OCI_DATABASE_PASSWORD=COLOQUE_A_SENHA_APENAS_LOCALMENTE
OCI_WALLET_PATH=C:/caminho/para/wallet-techmind

# Keep the local mock enabled unless the Python ML sidecar is available.
ML_SERVICE_MOCK_ENABLED=true
